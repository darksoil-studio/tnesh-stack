# {{pascal_case app_name}}Store

The `{{pascal_case app_name}}Store` is a typescript class that contains [async signals](https://www.npmjs.com/package/async-signals), which you can watch to get reactive updates in your elements.

```js
import { {{pascal_case app_name}}Store, {{pascal_case app_name}}Client } from "@darksoil-studio/{{kebab_case app_name}}";
const store = new {{pascal_case app_name}}Store(new {{pascal_case app_name}}Client(appClient, 'my-role-name'));
```

> Learn how to setup the `AppClient` object [here](https://www.npmjs.com/package/@holochain/client).

Learn more about the stores and how to integrate them in different frameworks [here](https://darksoil.studio/tnesh-stack).
